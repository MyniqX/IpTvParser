<UserControl x:Class="IpTvParser.Controls.IconButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IpTvParser.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="150">
    <Button>
        <Button.Content>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Image Source="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" Width="15" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center"/>
            </StackPanel>
        </Button.Content>
    </Button>
</UserControl>
